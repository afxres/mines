<Window
    x:Class="Mikodev.Mines.Viewer.MainWindow"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Mines"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
  <Grid>
    <Grid ColumnDefinitions="* 16 Auto" Margin="16">
      <Viewbox x:Name="viewer" Grid.Column="0" />
      <Grid Grid.Column="2" Width="240">
        <StackPanel VerticalAlignment="Center">
          <TextBlock x:Name="marker" />
          <TextBlock x:Name="ticker" />
          <TextBlock x:Name="banner" />
          <Button x:Name="reopen">New Game</Button>
        </StackPanel>
      </Grid>
    </Grid>
  </Grid>
</Window>
